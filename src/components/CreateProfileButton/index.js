import React, { useState } from 'react';

const CreateProfileButton = () => {
    const [state, setModalState] = useState(initialState)
    return ( 
        <h1> hello </h1>
     );
}
 
export default CreateProfileButton;